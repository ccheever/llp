name: ref-check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ref-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: This repo must pass
        run: ./ref-check --verbose
      - name: The broken-ref fixture must fail
        run: "! ./ref-check --root fixtures/broken-ref"
